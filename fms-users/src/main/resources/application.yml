management:
  endpoints:
    web:
      exposure:
        include: '*'
        
spring:
  application:
    name: fms-users
  config:
    import: 
    - optional:configserver:http://config-server:8888/
  profiles:
    active: test

