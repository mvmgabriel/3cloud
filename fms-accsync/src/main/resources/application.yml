eureka:
  client:
    instance:
      preferIpAddress: true
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
      
server:
  port: 8080
  servlet:
    context-path: /fms-accsync-api
    
spring:
  application:
    name: fms-accsync-api
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://zipkin:9411
