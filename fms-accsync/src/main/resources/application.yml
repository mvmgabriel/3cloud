management:
  endpoints:
    web:
      exposure:
        include: '*'
        
spring:
  application:
    name: fms-accsync-api
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://zipkin:9411
  config:
    import: 
    - optional:configserver:http://config-server:8888/
  profiles:
    active: test
