management:
  endpoints:
    web:
      exposure:
        include: '*'
        
spring:
  application:
    name: fms-instruments
  config:
    import: 
    - optional:configserver:http://localhost:8888/
  profiles:
    active: prod
